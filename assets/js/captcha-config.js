/* =============================================================
   Рельс-Комплект — Конфигурация Yandex SmartCaptcha
   Загружается на КАЖДОЙ странице с формами ПЕРЕД main.js.

   Если RK_CAPTCHA_SITEKEY === '' — капча отключена (graceful):
     • виджет не рендерится;
     • токен в payload не отправляется;
     • backend (notify_app.py) при пустом SMARTCAPTCHA_SECRET тоже
       пропускает заявки.

   Когда заказчик зарегистрируется и получит ключи:
     1. Вписать sitekey в RK_CAPTCHA_SITEKEY ниже.
     2. На сервере добавить в .env: SMARTCAPTCHA_SECRET=...
     3. Перезапустить rels-notify.service.
   ============================================================= */

'use strict';

window.RK_CAPTCHA_SITEKEY = '';  // ← вписать после регистрации
